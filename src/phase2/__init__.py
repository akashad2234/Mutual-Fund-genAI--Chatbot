"""
Phase 2: knowledge store and indexing.

This phase takes the structured JSON output from Phase 1 and:
- Loads it into a relational store (SQLite for local development).
- Builds a simple chunked index suitable for later RAG retrieval.
"""

