"""
Phase 5: backend services.

This phase exposes the core chatbot logic via an HTTP API, primarily:
- POST /chat: takes a user message and returns an answer, sources,
  question type, and resolved funds.
"""

